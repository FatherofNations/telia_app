<?xml version = "1.0" encoding = "utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:backgroundTint="#000000"
    tools:context=".MainActivity">


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center">

        <TextView
            android:id="@+id/text"
            android:layout_width="301dp"
            android:layout_height="164dp"
            android:background="#00EA1717"
            android:backgroundTint="#000000"
            android:shadowColor="#C51F1F"
            android:textColor="#000000"
            android:textSize="30sp"
            tools:text="tyherg" />

    </LinearLayout>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerInParent="true"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/speak"
            android:layout_width="114dp"
            android:layout_height="108dp"
            android:background="?selectableItemBackground"
            android:contentDescription="@string/todo"
            android:scrollbarThumbHorizontal="@drawable/ic_baseline_mic_none_24"
            android:scrollbarThumbVertical="@drawable/ic_baseline_mic_none_24"
            android:scrollbarTrackVertical="@drawable/ic_baseline_mic_none_24"
            android:src="@android:drawable/ic_btn_speak_now"
            app:srcCompat="@drawable/ic_baseline_mic_none_24"
            tools:srcCompat="@drawable/ic_baseline_mic_none_24" />

        <Space
            android:layout_width="match_parent"
            android:layout_height="88dp" />

    </LinearLayout>

</RelativeLayout>
